# 浮光笔记 — 项目维护指南

> 最后更新：2026-07-24

## 项目概览

| 项目 | 说明 |
|------|------|
| **名称** | 浮光笔记（FUGUANG NOTES） |
| **框架** | [vinext](https://github.com/cloudflare/vinext)（基于 React 19 + Vite 8 的 RSC 全栈框架） |
| **运行时** | Cloudflare Workers |
| **数据库** | Cloudflare D1（SQLite 兼容，Drizzle ORM） |
| **样式** | Tailwind CSS 4 |
| **认证** | ChatGPT 登录（OpenAI Sites）+ `ADMIN_EMAILS` 白名单；独立 Workers 部署已做兼容 |
| **Node.js** | >= 22.13.0 |

## 项目结构

```
gm-2/
├── app/                    # 应用页面 & API 路由（vinext App Router）
│   ├── admin/              # 写作后台页面
│   │   ├── page.tsx        # 后台入口（需认证）
│   │   ├── AdminStudio.tsx # 编辑器主组件
│   │   └── CopyEditor.tsx  # 站点文案编辑器
│   ├── api/admin/          # 后台 API（笔记增删改查、站点文案、批量操作）
│   ├── api/notes/          # 公开笔记 API
│   ├── api/site-copy/      # 公开站点文案 API
│   ├── archive/            # 归档页
│   ├── notes/[slug]/       # 笔记详情页
│   ├── rss.xml/            # RSS 订阅
│   ├── sitemap.xml/        # 站点地图
│   ├── chatgpt-auth.ts     # ★ 认证逻辑（AdminUser、ChatGPT 登录）
│   ├── lib/site-copy.ts    # 站点默认文案常量
│   └── components/         # 共享组件
├── db/                     # 数据库层
│   ├── schema.ts           # Drizzle schema 定义（notes、site_settings 表）
│   ├── index.ts            # getDb() 数据库实例
│   ├── notes.ts            # 笔记查询函数
│   └── site-copy.ts        # 站点文案读写函数
├── drizzle/                # Drizzle 迁移文件（SQL）
├── worker/
│   └── index.ts            # Cloudflare Worker 入口（fetch handler、迁移、图片优化）
├── build/
│   └── sites-vite-plugin.ts # vinext 构建插件（打包 hosting.json + 迁移文件）
├── public/                 # 静态资源（图片、字体）
├── scripts/                # 测试与审计脚本
├── tests/                  # 单元测试 + E2E
├── wrangler.jsonc          # ★ Cloudflare 部署配置（唯一配置来源）
├── vite.config.ts          # ★ Vite 构建配置
├── drizzle.config.ts       # Drizzle Kit 配置
├── package.json            # 依赖与脚本
└── .openai/
    └── hosting.json        # OpenAI Sites 托管声明（d1/r2 绑定名）
```

## 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器（HMR 热更新）
npm run dev
```

开发模式下，认证会自动绕过（`chatgpt-auth.ts` 中 `NODE_ENV !== "production"` 时会返回 `dev@localhost` 用户），无需配置 `ADMIN_EMAILS`。

D1 数据库在本地开发时由 Miniflare 模拟（内存 SQLite），**不会影响生产数据**。

## 构建

```bash
npm run build
```

构建过程分 5 步（由 vinext 驱动）：
1. 分析客户端引用
2. 分析服务端引用
3. 构建 RSC 环境
4. 构建客户端环境（`dist/client/`，包含静态资源）
5. 构建 SSR 环境（`dist/server/`，包含 Worker 入口）

产物：
- `dist/client/` — 客户端静态资源（692 个文件，会部署到 Cloudflare Assets）
- `dist/server/` — Worker 代码 + 自动生成的 `wrangler.json`
- `dist/.openai/` — OpenAI Sites 元数据

### 构建警告说明

构建时会出现大量 `FILE_NAME_CONFLICT` 警告（KaTeX 字体文件命名冲突），**可以忽略**——这是 vinext 构建已知问题，不影响功能。

## 部署到 Cloudflare

### 首次部署前准备

确保你的 Cloudflare 账号已有 D1 数据库。如果还没有，用 Wrangler 创建一个：

```bash
npx wrangler d1 create zc_blog
```

然后更新 `wrangler.jsonc` 里的 `database_id`。

### 设置环境变量

```bash
# 管理员邮箱白名单（必填）
npx wrangler secret put ADMIN_EMAILS

# 后台登录密码（强烈建议，用于独立登录页验证）
npx wrangler secret put ADMIN_PASSWORD
```

- `ADMIN_EMAILS`：输入你的邮箱（多个用英文逗号分隔）。**不设置后台无法访问。**
- `ADMIN_PASSWORD`：设置后台登录密码。设置后，点击"写作后台"会跳转到 `/admin/login` 登录页，输入密码后签发 7 天有效的 httpOnly session cookie。**不设置则无密码保护。**

### 部署命令

```bash
npm run build
npx wrangler deploy
```

部署成功后访问 `https://gm-2.<你的用户名>.workers.dev`。

> **注意**：首次部署后 SSL 证书需要几分钟生效，如果遇到 `ERR_SSL_VERSION_OR_CIPHER_MISMATCH`，等一两分钟再试。

### 部署更新

每次修改代码后：

```bash
# 1. 构建
npm run build

# 2. 部署
npx wrangler deploy
```

如果只改静态内容、没动依赖，Wrangler 会自动检测哪些文件变了，只上传差异部分。

## 环境变量

| 变量 | 必填 | 说明 |
|------|------|------|
| `ADMIN_EMAILS` | **是** | 生产环境下允许访问后台的邮箱白名单，逗号分隔。不设置则后台拒绝所有访问 |
| `ADMIN_PASSWORD` | 建议 | 独立部署模式的后台登录密码。设置后 `/admin` 会重定向到独立的登录页面，密码通过 HMAC-SHA256 签发的 httpOnly cookie 验证（7天有效期） |
| `NODE_ENV` | 否 | 构建时自动设为 `production`；本地 `npm run dev` 时为 `development` |

用 `npx wrangler secret put <变量名>` 设置，或直接在 `wrangler.jsonc` 中加 `vars` 字段。

## D1 数据库

### 表结构

两表一索引：

```sql
-- 笔记表
notes (
  id, slug, title, summary, content, category,
  status, featured, author_email, published_at,
  source_path, links_json, created_at, updated_at
)

-- 站点文案表（单行配置）
site_settings (
  id, copy_json, updated_by, updated_at
)
```

表在 Worker 首次请求时自动创建（`worker/index.ts` 的 `runMigrations()`），无需手动建表。

### 本地查看数据

```bash
# 启动本地开发服务器后，可以用 Wrangler 直接查 D1
npx wrangler d1 execute zc_blog --local --command "SELECT * FROM notes"
```

### 生产环境查询

```bash
npx wrangler d1 execute zc_blog --command "SELECT count(*) FROM notes"
```

### 修改 Schema

1. 修改 `db/schema.ts`
2. 生成迁移文件：`npm run db:generate`
3. 构建并部署。Worker 启动时会自动运行迁移

## 认证机制说明

所有认证逻辑在 `app/chatgpt-auth.ts`。认证页面和 API：

| 路由 | 说明 |
|------|------|
| `/admin/login` | 独立登录页面（`app/admin/login/page.tsx`） |
| `/api/admin/login` | 登录 API（`app/api/admin/login/route.ts`），验证密码并签发 cookie |
| `/api/admin/logout` | 退出 API（`app/api/admin/logout/route.ts`），清除 cookie |

### 两种部署模式

| 模式 | 认证方式 | `/admin` 访问 |
|------|---------|-------------|
| **OpenAI Sites**（`chatgpt.site` 域名） | ChatGPT 登录 + `ADMIN_EMAILS` 白名单 | 需先登录 ChatGPT，且邮箱在白名单中 |
| **独立 Workers 部署**（`workers.dev` 域名） | 仅 `ADMIN_EMAILS` 白名单 | 只要 `ADMIN_EMAILS` 已配置即可访问 |

独立部署模式下，Worker 层不再做验证，而是将 auth 交给应用层处理：

1. 点击"写作后台" → `requireAdminUser()` 检测到无 session
2. 如果 `ADMIN_PASSWORD` 已配置 → 重定向到 `/admin/login` 独立登录页
3. 用户输入密码 → POST `/api/admin/login` → 验证通过 → 签发 HMAC 签名的 `admin_session` cookie（httpOnly, 7天有效）→ 重定向回后台
4. 后续请求自动携带 cookie，`getChatGPTUser()` 验证 HMAC 签名通过 → 返回管理员用户
5. 点击"退出" → POST `/api/admin/logout` → 清除 cookie → 回到首页

安全性：
- Cookie 用 `ADMIN_PASSWORD` 作为密钥做 HMAC-SHA256 签名，无法伪造
- httpOnly + Secure + SameSite=Lax，防止 XSS 和 CSRF
- 不设置 `ADMIN_PASSWORD` 则不启用登录页（保持原行为）

### Auth 文件位置

所有认证逻辑在 `app/chatgpt-auth.ts`：

- `getChatGPTUser()` — 获取当前用户（开发模式/独立部署/ChatGPT 三种来源）
- `requireChatGPTUser(returnTo)` — 要求 ChatGPT 登录，否则重定向
- `getAdminUser()` — 检查当前用户是否在 `ADMIN_EMAILS` 白名单
- `requireAdminUser(returnTo)` — 后台页面入口认证

## 常用命令速查

```bash
npm run dev          # 本地开发
npm run build        # 构建
npx wrangler deploy  # 部署

npm run test         # 构建 + 运行单元测试
npm run test:unit    # 仅单元测试
npm run test:e2e     # E2E 测试（Playwright）
npm run lint         # ESLint 检查

npm run db:generate  # 生成 Drizzle 迁移文件
npx wrangler d1 execute zc_blog --command "SQL"  # 执行 SQL 查询
```

## Obsidian、GitHub 与线上博客同步

本项目把 `D:\Note-Repository` 作为本地写作目录，把 `content/notes/` 作为 GitHub 中可审计的公开内容快照，并把 Cloudflare D1 作为线上博客的运行时内容源。同步是非对称双向的：

```text
本地发布：D:\Note-Repository → GitHub → Cloudflare D1
后台保存：写作后台 → Cloudflare D1（立即展示）→ GitHub
本地拉取：GitHub → D:\Note-Repository（仅手动执行）
```

只有 frontmatter 中包含以下任意一个布尔标记的 Markdown 会进入公开同步：

```yaml
---
blog: true
title: 示例文章
date: 2026-08-10
category: 技术
tags:
  - Obsidian
  - Cloudflare
featured: false
---
```

也支持用 `publish: true` 或 `published: true` 代替 `blog: true`。没有公开标记的私人笔记不会离开 Vault。

同步命令：

```bash
# 只检查解析结果，不写文件、不连接线上服务
npm run obsidian:check

# 更新仓库中的公开内容快照，但不推送、不修改线上博客
npm run obsidian:sync

# 仅上传博客（主要用于本地演练或 GitHub 已同步后的重试）
npm run obsidian:upload

# 手动把后台/GitHub 的更新拉到本地 Vault
npm run obsidian:pull

# 依次更新内容快照、推送 GitHub、更新线上 D1
npm run obsidian:publish
```

生产发布复用博客已有的管理员登录。执行前在当前终端临时设置密码（不要写进配置文件或提交 Git）：

```powershell
$env:BLOG_ADMIN_PASSWORD = "你的 ADMIN_PASSWORD"
npm run obsidian:publish
Remove-Item Env:BLOG_ADMIN_PASSWORD
```

安全约定：

- `obsidian:publish` 默认不删除或下架任何线上文章。
- `obsidian:pull` 是 GitHub/后台内容进入本地 Vault 的唯一入口，后台保存不会直接连接你的电脑。
- 手动拉取使用 `.obsidian/blog-sync-state.json` 检测双向修改；如果本地和后台同时改了同一篇文章，会报告冲突而不覆盖本地文件。
- GitHub 已删除而本地未修改的文章，只有在 `obsidian:pull -- --prune` 时才会移动到 Vault 的 `.trash/blog-sync/`，可以恢复。
- 只有显式附加 `-- --prune` 才会把已由同步工具管理、但不再存在于公开清单中的文章改为草稿。
- 发布前如果仓库中存在内容快照之外的未提交修改，命令会停止，避免意外提交代码。
- 笔记通过 Vault 相对路径 `source_path` 保持稳定身份；改标题不会创建第二篇文章。
- 新增本地图片会被内容哈希命名并复制到 `public/obsidian-assets/`；发布命令检测到新图片时会自动重新部署静态资源。
- 可通过 `-- --vault "其他路径"` 临时覆盖 Vault 路径，或设置 `OBSIDIAN_VAULT_PATH`。

配置文件为 `obsidian-sync.config.json`。首次在空 Vault 中恢复当前线上文章时运行：

```bash
npm run obsidian:bootstrap
```

### 后台自动同步 GitHub

后台对 D1 的保存会立即生效。已发布文章随后自动写入 GitHub 的 `content/notes/`；保存草稿、下架或删除文章时，对应的公开 GitHub 快照会被移除。由于 GitHub 仓库是公开的，未发布草稿不会提交。

自动写 GitHub 需要一个只授权此仓库的 fine-grained personal access token：

1. 在 GitHub 创建 fine-grained token，只选择 `Zhou-chu/Zhou-chu.github.io`。
2. Repository permissions 只授予 `Contents: Read and write`。
3. 在项目目录执行以下命令，并在 Wrangler 的隐藏输入提示中粘贴 token：

```bash
npx wrangler secret put GITHUB_CONTENT_TOKEN
```

不要把 token 写入 `.env`、配置文件、笔记或 Git。未配置令牌时，网站保存仍然成功，后台会提示“GitHub 自动同步尚未配置”；GitHub API 暂时失败时，也会明确提示网站已更新但 GitHub 未同步。

## 常见问题

### 1. 部署报错 `D1 binding 'DB' references database '...000000' which was not found`

**原因**：`vite.config.ts` 中的 `localBindingConfig` 和 `wrangler.jsonc` 各定义了一份 D1 配置，vinext 构建时把它们合并了，其中 `vite.config.ts` 里的 `database_id` 是占位符 `00000000-0000-4000-8000-000000000000`。

**修复**：已修复。`vite.config.ts` 的 `d1_databases` 置空，只保留 `wrangler.jsonc` 作为唯一 D1 配置来源。

### 2. 部署报错 `DB assigned to multiple D1 Database bindings`

**原因**：同上，两份配置的 `binding` 名称不同导致重复条目。

**修复**：已修复。`wrangler.jsonc` 的 `binding` 从 `"env.DB"` 改为 `"DB"`（Cloudflare Workers 中 `env.DB` 对应的 binding 名就是 `DB`）。

### 3. 部署报错 `Compatibility flag specified multiple times: nodejs_compat`

**原因**：`vite.config.ts` 的 `localBindingConfig` 和 `wrangler.jsonc` 都声明了 `nodejs_compat`。

**修复**：已修复。移除了 `vite.config.ts` 中的 `compatibility_flags`，由 `wrangler.jsonc` 统一管理。

### 4. 访问 `/admin` 提示"页面未找到"

**原因**：在没有 OpenAI Sites 的独立 Workers 部署上，`requireAdminUser()` 会重定向到 `/signin-with-chatgpt`（ChatGPT 登录页），但 Workers 上没有这个路由，导致 404。

**修复**：已修复。`getChatGPTUser()` 现在检测到 `ADMIN_EMAILS` 已配置但无 ChatGPT header 时，自动 fallback 到独立部署模式。

### 5. 部署后 SSL 报错

**原因**：`workers.dev` 子域名证书需要几分钟生成。

**解决**：等 1-2 分钟后刷新，或用 `curl` 测试。

### 6. 构建时大量 FILE_NAME_CONFLICT 警告

**原因**：KaTeX 的字体文件在客户端和服务端构建时被重复生成。

**处理**：忽略，不影响功能。vinext 已知问题。

## 自定义域名（可选）

1. 在 Cloudflare Dashboard 中添加你的域名
2. 在 Workers 设置中绑定自定义域名
3. DNS 指向 Cloudflare

部署到自定义域名后，建议启用 OpenAI Sites 部署以获得 ChatGPT 登录支持。否则继续使用独立部署模式（`ADMIN_EMAILS` fallback）。

## 更新依赖

```bash
# 查看可更新的依赖
npm outdated

# 更新 vinext 及相关包
npm install vinext@latest @cloudflare/vite-plugin@latest wrangler@latest

# 更新后务必测试
npm run build
npm test
```

**注意**：更新 `vinext` 或 `@cloudflare/vite-plugin` 后，检查 `vite.config.ts` 中的 `localBindingConfig` 是否与新版兼容，以及构建产物是否有变化。
